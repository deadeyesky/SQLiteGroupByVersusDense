# SQLite `GROUP BY` vs `DENSE_RANK`

Simple benchmark of SQLite3 using Python to test comparable queries for a test case

## How to run

1. Make sure that you have uv installed.
2. After installing it, 
